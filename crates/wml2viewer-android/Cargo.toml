[package]
name = "wml2viewer-android"
version = "0.0.20"
edition = "2024"
license = "MIT"
description = "JNI bridge between the Android Compose application and wml2viewer-core"
publish = false

[lib]
name = "wml2viewer_android"
crate-type = ["cdylib", "rlib"]

[dependencies]
jni = "0.22.4"
wml2viewer-mobile-bridge = { path = "../wml2viewer-mobile-bridge" }
