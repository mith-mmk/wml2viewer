org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
# protobuf-gradle-plugin 0.9.5 still reads BaseExtension under AGP 9.
# Keep AGP 9 built-in Kotlin enabled while exposing the transitional DSL bridge.
android.newDsl=false
kotlin.code.style=official
