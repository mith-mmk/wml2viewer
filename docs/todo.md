# wml2viewer TODO
- project wml2viewerに関するもののみ

ステータス
- [x] 確認済み / 安定実装
- [+] 実装済み / 今後の拡張余地あり
- [*] 実装済みだが要再確認 or 既知の不具合あり
- [-] 設計保留
- [ ] 未実装

最終整理日: 2026-04-18

# 0.0.15
今回対応範囲（このターン）
- [x] test filesystem::tests::listed_file_returns_to_directory_on_next_and_prev の不具合修正(テスト側を修正: フラットナビゲーションは.wmltxtを展開しない設計に合わせた)
1. [*] キーバインドのカスタマイズ 
  - [x] デフォルト・キーバインドの変更
  - [*] 設定UI（初心者向けフォーム）
    - [x] |ファンクション| **101キー以上**のキーイベントの組み合わせをフォームで設定できる
    - [x] キー入力イベントを拾ってキー欄へ自動挿入するインプットボックス
    - [x] 日本語リソースの追加
    - [x] マウスイベントのマッピング
    - [x] デフォルトマッピング一覧を設定画面に最初から表示させる（デフォルトは変更される予定）
      - [x] 表示を表形式に変更し、スクロール可能にする
      - [+] 次の画面の場合、space allow 左クリックが割り当てられているイベントをすべて表示する
      - [x] キーイベントを入力するとき、他のフィールドに移動するときのprimary touchを拾って上書きしてしまう問題
      - [x] primay touchなどのイベント名が日本語になっていないのでリソースの追加
      - [x] F1 = Help/ ALT + F4/ F5は変更できない。変更できないようにロックする
      - [x] CTRL, ALT, SHIFTにしているが、 MACではCTRL -> COMMAND、ALT -> OPTION、SHIFTとして表示を変える
  - [+] カスタマイズの実装チェック
  - [ ] HELPの更新

2. メニューの部分は枠だけ作ってテンプレートかjsonで管理するようにして、コンパイル時に読み込む形にしたい
   - [ ] リソースはjsonで管理できるようにする。場所はsettingsディレクトリの下にi18n/[resource_name]/message.json
   - [ ] メニューの内容と配置を簡単に変更できる用にする
   - [ ] 多国語対応は手動でやってもらう形で、リソースのjsonファイルを用意するだけにする
   - [ ] --system templateでひな形を出力する

3. [ ] 追加ファンクション move, copy ,delete(trash), rename
    — [ ] ファイル -> 再読み込み/移動/コピー/削除/リネーム
    - 再読み込みはF5に割り当てる`変更不可`
    - [ ] 削除 はゴミ箱に移動する形で実装する。ゴミ箱に移動できない場合は完全削除する　削除前に確認ｓるダイアログを表示する
    - [ ] 移動 とコピーは、移動先のパスを入力するダイアログを表示する。defaultは設定で設定可能
    - [ ] 切り替え 移動とコピーの設定切り替えをワンクリックでできるようにする(function set_move_folder1, set_move_folder2, set_copy_folder1, set_copy_folder2 などを用意して、設定画面で切り替えられるようにする) 
    - [ ] 移動とコピーの設定は、ユーザーが簡単に理解できる形式で行う
    - [ ] リネームは、リネーム後のファイル名を入力するダイアログを表示する。defaultは現在のファイル名で、拡張子は変更できないようにする

4. [+] 右クリックメニューの変更
   1. [ ] 見栄えに問題あり
    ```
    ファイル -> 再読み込み/移動/コピー/削除
    画像 -> フォーマットを変更して保存
    表示 -> 拡大/縮小/倍率を指定/原寸大/漫画モードのオン・オフ
    画像の情報 -> metadataを整形して表示
    設定 -> 設定画面へ
    このプログラムについて 
    ```
5. [ ] 画像の情報 -> metadataを整形して表示
    - [x] 画像の幅、高さ、ファイルサイズ、フォーマット、カラープロファイル、EXIF情報などを表示する
    - [x] 可能であれば、画像の作成日時や更新日時も表示する
    - [x] メタデータの表示は、ユーザーが簡単に理解できる形式で行う（例: テーブル形式やリスト形式）
    - [ ] EXIF情報の表示は、必要に応じてカテゴリ分けして表示する（例: カメラ情報、撮影条件、位置情報など）
    - [x] issue: \\が \\\\ と表示される問題の修正

6. [ ] Linuxにおけるプラグインメニューの表示
   - [ ] susie64とsystemがないので表示させない
7. [ ] ファイラー／サブファイラー／サムネイルの表示 なるべく固定位置に

